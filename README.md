# Mini Shell (C++ Command Line Interpreter)

A custom shell implemented in C++ demonstrating:
- Command parsing
- Process creation (`fork()`, `execvp()`)
- Pipes and I/O redirection
- Built-in commands (`cd`, `help`, `exit`)

## Project Structure
See the file tree above.

## Build & Run
```bash
make
./bin/shell
